# Шаблон документа для генерации API-документации на основе openapi.json

Этот файл содержит **полный шаблон (template)**, который должен использоваться при генерации документации API.  

---

# 📘 **Шаблон API-документации**

## **ИНТЕРФЕЙСЫ ВЗАИМОДЕЙСТВИЯ — <Название_группы/тега>**

Документ генерируется для каждого тега из OpenAPI.  
Если тегов нет — используется общий заголовок:  
**ИНТЕРФЕЙСЫ ВЗАИМОДЕЙСТВИЯ — API**

---

# ## **<Номер>. <Название метода (summary / operationId)>**

*(Название не переводится, берётся из OpenAPI.)*

---

## **1. Описание**

Краткое описание назначения метода.  
Основа — `description` из OpenAPI.  
Если отсутствует — формируется на основе URL и типа операции.

---

## **2. Требования к интерфейсу**

| Параметр | Значение |
|---------|----------|
| Синхронный/Асинхронный | |
| Технология | REST API (HTTP request–response) |
| Время ответа | Не более 1 секунды |
| Формат ответа | JSON |
| Кодировка | UTF-8 |
| Аутентификация | OAuth2PasswordBearer (если в OpenAPI иное — указать) |

---

## **3. Формат запроса**

| Поле | Значение |
|------|----------|
| **URL** | `<полный путь (path)>` |
| **Метод** | `<HTTP-метод>` |

---

## **4. Параметры запроса**

Общая таблица всех параметров: path, query, header, cookie, body.

| Имя | Где | Тип | Описание | Обязательный |
|-----|-----|-----|-----------|--------------|
| `<field>` | path/query/header/cookie/body | `<тип>` | `<описание>` | Да/Нет |

Если метод принимает `requestBody`, добавить строки:

- `—` | body | object | Тело запроса | Да/Нет  
и далее — поля объекта (если нужно):

| `<body.field>` | body | `<тип>` | `<описание>` | Да/Нет |

---

## **5. Формат ответа**

Таблица полей основного объекта ответа (`200` или `201`):

| Поле | Тип | Описание |
|------|------|----------|
| `<response.field>` | `<тип>` | `<описание>` |

Если ответ — массив: описывается объект элемента массива.

При отсутствии в OpenAPI — добавлять:

| errorCode | Integer | Код ошибки (0 — нет ошибки) |
| errorMessage | String | Сообщение об ошибке |

---

## **6. Пример запроса (JSON)**

```json
{
  "field": "value",
  "another": 123
}
```

Если GET и нет тела — можно привести пример формата query:

```json
{
  "limit": 10,
  "offset": 0
}
```

---

## **7. Пример ответа (JSON)**

```json
{
  "id": 123,
  "name": "example",
  "errorCode": 0,
  "errorMessage": ""
}
```

Для массивов:

```json
[
  {
    "id": 1,
    "name": "item1"
  },
  {
    "id": 2,
    "name": "item2"
  }
]
```

---

## **8. Примеры ошибок**

```json
{
  "error": "Invalid request",
  "code": 400
}
```

```json
{
  "error": "Unauthorized",
  "code": 401
}
```

```json
{
  "error": "Internal server error",
  "code": 500
}
```

---

